import { EnableRoleDirective } from './enable-role.directive';

describe('EnableRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new EnableRoleDirective();
    expect(directive).toBeTruthy();
  });
});
